export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
  WORKER = 'WORKER',
}
export enum AnimalType {
  DOG = 'dog',
  CAT = 'cat',
}

export enum AnimalStatus {
  AVAILABLE = 'available',
  ADOPTED = 'adopted', 
  FOSTERING ='fostering'

}