
export enum UserRole {
  ADMIN = 'ADMIN',
  USER = 'USER',
  WORKER = 'WORKER',
}

export enum AnimalType {
  DOG = 'dog',
  CAT = 'cat',
}

export enum AnimalStatus {
  AVAILABLE = 'available',
  PENDING = 'pending',
  ADOPTED = 'adopted'
}

export enum AdoptionStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  REJECTED = 'rejected'
}